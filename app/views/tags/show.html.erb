<h1>Articles Tagged with <%= @tag.name %></h1>

<ul>
  <% @tag.articles.each do |article| %>
    <%= link_to article_path(article) do %>
      <%= fa_icon "book", text: article.title %>
    <% end %>
  <% end  %>
</ul>
